services:
  nginx:
    image: nginx:alpine
    container_name: nginx-website
    ports:
      - "80:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
    restart: unless-stopped
